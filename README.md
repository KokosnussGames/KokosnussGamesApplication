# Kokosnuss Games Application

Includes three games: Sudoku, Minesweeper, and Nonogram.

Start the full app with Docker Compose:

```bash
docker compose build
docker compose up
```

Open the app at:

```text
http://localhost:8080
```

Stop it with `Ctrl+C`, or run this from another terminal:

```bash
docker compose down
```
